import { moduleFor, test } from 'ember-qunit';

moduleFor('view:places/index/new', 'Unit | View | places/index/new');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
